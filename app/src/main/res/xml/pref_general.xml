<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <ListPreference
        android:defaultValue="3"
        android:entries="@array/prefs_color"
        android:entryValues="@array/prefs_color"
        android:key="list_preference_color"
        android:summary="%s"
        android:title="@string/prefs_color_title" />
    <ListPreference
        android:defaultValue="0"
        android:entries="@array/prefs_order"
        android:entryValues="@array/prefs_order"
        android:key="list_preference_order"
        android:summary="%s"
        android:title="@string/prefs_order_title" />
</PreferenceScreen>
